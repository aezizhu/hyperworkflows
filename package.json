{
  "name": "hyperworkflows",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "Evidence-factory orchestration plugin for Claude Code",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "test": "node --test",
    "bundle": "node scripts/bundle-workflows.mjs --write",
    "bundle:check": "node scripts/bundle-workflows.mjs --check",
    "check:engines": "node scripts/check-engines.mjs workflows/*.js",
    "check": "npm run bundle:check && npm run check:engines && node --test"
  },
  "license": "MIT"
}
